#!/usr/bin/env node

/**
 * Script para atualizar o .env com a URL correta do ngrok
 * COMENTADO - usando apenas localhost
 * 
 * Para usar ngrok no futuro, descomente o código abaixo
 */

console.log('✅ Using localhost only (ngrok disabled)');
